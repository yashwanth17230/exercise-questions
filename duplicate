#include<stdio.h>
void main()
{
    int n,i,j,a[10],flag=0;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
        scanf("%d",&a[i]);
     for(i=0;i<=n;i++)
     {
         for(j=0;j<=i;j++)
         {
           if(a[i]==j)
           {
                flag++;
           }
         }
     }
                 
   
        
    
    if (flag>1)
    printf("duplicat=%d",flag);
    else
    printf("no duplicate");
}
